name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  NODE_VERSION: '18'

jobs:
  test:
    name: Test and Lint
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Type check
      run: npm run check
      continue-on-error: true

    - name: Lint code
      run: npm run lint
      continue-on-error: false

    - name: Format check
      run: npm run format:check
      continue-on-error: false

    - name: Run tests
      run: npm run test:run
      continue-on-error: true
      env:
        NODE_ENV: test
        DATABASE_URL: sqlite://test.db
        OPENAI_API_KEY: sk-proj-test-key-for-ci-cd-pipeline-testing
        MAPBOX_ACCESS_TOKEN: pk.eyJ1IjoidGVzdCIsImEiOiJ0ZXN0LWtleSJ9.test
        SESSION_SECRET: test-session-secret-for-ci-cd

    - name: Build application
      run: npm run build
      env:
        NODE_ENV: production
        DATABASE_URL: postgresql://test:test@localhost:5432/test
        OPENAI_API_KEY: sk-proj-test-key-for-ci-cd-pipeline-testing
        MAPBOX_ACCESS_TOKEN: pk.eyJ1IjoidGVzdCIsImEiOiJ0ZXN0LWtleSJ9.test
        SESSION_SECRET: test-session-secret-for-ci-cd

  security:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'

    - name: Install dependencies
      run: npm ci

    - name: Run security audit
      run: npm audit --audit-level=high
      continue-on-error: true

  build-docker:
    name: Build Docker Image
    runs-on: ubuntu-latest
    needs: [test, security]
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Login to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        platforms: linux/amd64,linux/arm64
        push: true
        tags: |
          ${{ secrets.DOCKER_USERNAME }}/techassist-ai:latest
          ${{ secrets.DOCKER_USERNAME }}/techassist-ai:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max